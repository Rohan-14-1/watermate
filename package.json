{
  "name": "watermate",
  "version": "1.0.0",
  "description": "Roommate water management system - track whose turn it is to bring drinking water.",
  "main": "backend/server.js",
  "scripts": {
    "dev": "nodemon backend/server.js",
    "start": "node backend/server.js",
    "build": "node backend/prisma/db-deploy.js && mkdir -p public && cp -r frontend/* public/",
    "vercel-build": "node backend/prisma/db-deploy.js && mkdir -p public && cp -r frontend/* public/",
    "postinstall": "prisma generate --schema=backend/prisma/schema.prisma",
    "prisma:generate": "prisma generate --schema=backend/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=backend/prisma/schema.prisma",
    "prisma:deploy": "prisma migrate deploy --schema=backend/prisma/schema.prisma",
    "prisma:push": "prisma db push --schema=backend/prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema=backend/prisma/schema.prisma",
    "seed": "node backend/prisma/seed.js",
    "mobile:sync": "cd mobile && npm run sync",
    "mobile:android": "cd mobile && npm run open:android",
    "mobile:ios": "cd mobile && npm run open:ios"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^14.3.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^5.19.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
